// App-wide, non-visual constants for Queenstown Rewards.
// Sample/placeholder values only — do not treat as real business policy
// until confirmed by the product owner.

export const APP_NAME = 'Queenstown Rewards';

// Sample data pending real branding/business details (see AGENTS.md, Phase 3+).
export const IS_SAMPLE_DATA = true;

// One free Burger of the Month redemption per eligible calendar month.
export const REWARD_PERIOD = 'monthly' as const;
export const MAX_REDEMPTIONS_PER_PERIOD = 1;
